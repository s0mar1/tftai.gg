{
  "query": "query GetSummonerIntegrated($name: String!, $region: String, $matchCount: Int) { summonerIntegrated(name: $name, region: $region, matchCount: $matchCount) { success message data { summoner { puuid name } recentMatches { gameId traits { name level } units { championId name items } } } } }",
  "variables": {
    "name": "hide on bush",
    "region": "kr",
    "matchCount": 1
  }
}